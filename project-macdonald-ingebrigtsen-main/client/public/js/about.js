/* window.addEventListener("load", eventObj => {
    const question = document.querySelector("#q1");

    question.addEventListener("click", showAnswer);


});

function showAnswer(eventObj) {
    const elemObj = eventObj.target;
    const text = elemObj.querySelector("p");
    text.style.visibility == "" ? text.style.visibility = "visible" : text.style.filter = "hidden";
    console.log(text.style.visibility);
    

}; */